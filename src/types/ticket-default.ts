export type TicketDefaultFormValues = {
  middleText: string
  bottomText: string
}

export const DEFAULT_TICKET_DEFAULT_FORM: TicketDefaultFormValues = {
  middleText: "All sales are final. No credits, refunds, or exchanges.",
  bottomText: "Bottom text under Price here.",
}
